% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RealMLP.R
\name{setRealMLP}
\alias{setRealMLP}
\title{Create RealMLP Settings}
\usage{
setRealMLP(
  numLayers = 3L,
  sizeHidden = 256L,
  dropout = 0.15,
  sizeEmbedding = 64L,
  labelSmoothing = 0,
  numericEmbeddingMode = "scale",
  numericNumFrequencies = 8L,
  numericPeriodicInitStd = 0.1,
  numericPbldHiddenDim = 16L,
  numericPbldEmbeddingDim = 4L,
  dataDependentInitMode = "paper_lsuv",
  dataDependentInitTargetVar = 1,
  dataDependentInitMaxRows = 65536L,
  dataDependentInitGainClip = 10,
  dataDependentInitBiasRefitSteps = 2L,
  scalingLrMult = 6,
  biasLrMult = 0.1,
  actLrMult = 0.1,
  embeddingLrMult = 0.1,
  paperMode = TRUE,
  tokenAggregation = "auto",
  featureScaleMode = "auto",
  device = "cpu",
  hyperParamSearch = "grid",
  randomSample = 100,
  randomSampleSeed = NULL
)
}
\arguments{
\item{numLayers}{hidden layers (default 3)}

\item{sizeHidden}{hidden width (default 256)}

\item{dropout}{base dropout p (default 0.15; scheduled with flat_cos)}

\item{sizeEmbedding}{embedding dim for compatibility with existing Embedding (default 64)}

\item{labelSmoothing}{epsilon for label smoothing (default 0.0 for AUROC mode)}

\item{numericEmbeddingMode}{numeric token embedding mode: "scale", "pl", or "pbld"}

\item{numericNumFrequencies}{periodic frequency count for PL/PBLD modes}

\item{numericPeriodicInitStd}{std used to initialize periodic frequencies}

\item{numericPbldHiddenDim}{hidden width for PBLD per-feature block}

\item{numericPbldEmbeddingDim}{low-dimensional PBLD output width before projection}

\item{dataDependentInitMode}{data-dependent init mode: "paper_lsuv" or "current"}

\item{dataDependentInitTargetVar}{target pre-activation variance per neuron}

\item{dataDependentInitMaxRows}{max sampled rows for init statistics (0 means all sampled rows)}

\item{dataDependentInitGainClip}{optional clip on LSUV gain multipliers (>1 enables clipping)}

\item{dataDependentInitBiasRefitSteps}{bias recenter iterations during data-dependent init}

\item{scalingLrMult}{LR multiplier for scaling parameters (default 6.0)}

\item{biasLrMult}{LR multiplier for bias parameters (default 0.1)}

\item{actLrMult}{LR multiplier for parametric activation parameters (default 0.1)}

\item{embeddingLrMult}{LR multiplier for embedding parameters (default 0.1)}

\item{paperMode}{if TRUE, enforce paper-aligned defaults where possible}

\item{tokenAggregation}{token aggregation mode: "auto", "mean", "sum", "sum_len_norm"}

\item{featureScaleMode}{feature scale mode: "auto", "scalar", "vector"}

\item{device}{"cpu" or "cuda" (default "cpu")}

\item{hyperParamSearch}{Which kind of hyperparameter search to use: random
sampling or exhaustive grid search. Default: "grid"}

\item{randomSample}{How many random samples from hyperparameter space to use
when \code{hyperParamSearch = "random"}}

\item{randomSampleSeed}{Random seed to sample hyperparameter combinations}
}
\value{
A \code{modelSettings} object for use with \code{PatientLevelPrediction}.
}
\description{
Creates model and hyperparameter-search settings for RealMLP binary
classification.
}
\details{
The implementation is based on
\href{https://arxiv.org/abs/2407.04491}{Holzmuller et al. (2024)}.
Preprocessing, including robust scaling and clipping, is handled upstream.
}
\examples{
realMlpSettings <- setRealMLP(
  sizeHidden = c(128, 256),
  dropout = c(0.1, 0.2),
  hyperParamSearch = "random",
  randomSample = 2,
  randomSampleSeed = 42
)
length(realMlpSettings$param)
}
