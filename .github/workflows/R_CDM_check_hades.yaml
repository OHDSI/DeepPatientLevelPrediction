# For help debugging build failures open an issue on the RStudio community with the 'github-actions' tag.
# https://community.rstudio.com/new-topic?category=Package%20development&tags=github-actions
on:
  push:
    branches:
      - '**'

name: macOS segfault isolate

jobs:
  isolate:
    runs-on: macOS-latest

    name: ${{ matrix.scenario }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - scenario: package-load
            andromeda: current
          - scenario: setup-through-dataset
            andromeda: current
          - scenario: setup-before-fit
            andromeda: current
          - scenario: setup-after-fit
            andromeda: current
          - scenario: setup-after-save
            andromeda: current
          - scenario: full-tests
            andromeda: current
          - scenario: setup-after-save-andromeda-1.2.0
            andromeda: 1.2.0
          - scenario: full-tests-andromeda-1.2.0
            andromeda: 1.2.0

    env:
      GITHUB_PAT: ${{ secrets.GH_TOKEN }}
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      CDM5_ORACLE_CDM_SCHEMA: ${{ secrets.CDM5_ORACLE_CDM_SCHEMA }}
      CDM5_ORACLE_OHDSI_SCHEMA: ${{ secrets.CDM5_ORACLE_OHDSI_SCHEMA }}
      CDM5_ORACLE_PASSWORD: ${{ secrets.CDM5_ORACLE_PASSWORD }}
      CDM5_ORACLE_SERVER: ${{ secrets.CDM5_ORACLE_SERVER }}
      CDM5_ORACLE_USER: ${{ secrets.CDM5_ORACLE_USER }}
      CDM5_POSTGRESQL_CDM_SCHEMA: ${{ secrets.CDM5_POSTGRESQL_CDM_SCHEMA }}
      CDM5_POSTGRESQL_OHDSI_SCHEMA: ${{ secrets.CDM5_POSTGRESQL_OHDSI_SCHEMA }}
      CDM5_POSTGRESQL_PASSWORD: ${{ secrets.CDM5_POSTGRESQL_PASSWORD }}
      CDM5_POSTGRESQL_SERVER: ${{ secrets.CDM5_POSTGRESQL_SERVER }}
      CDM5_POSTGRESQL_USER: ${{ secrets.CDM5_POSTGRESQL_USER }}
      CDM5_SQL_SERVER_CDM_SCHEMA: ${{ secrets.CDM5_SQL_SERVER_CDM_SCHEMA }}
      CDM5_SQL_SERVER_OHDSI_SCHEMA: ${{ secrets.CDM5_SQL_SERVER_OHDSI_SCHEMA }}
      CDM5_SQL_SERVER_PASSWORD: ${{ secrets.CDM5_SQL_SERVER_PASSWORD }}
      CDM5_SQL_SERVER_SERVER: ${{ secrets.CDM5_SQL_SERVER_SERVER }}
      CDM5_SQL_SERVER_USER: ${{ secrets.CDM5_SQL_SERVER_USER }}
      UV_SYSTEM_PYTHON: 1
      RETICULATE_USE_MANAGED_VENV: "no"
      DPLP_TEST_FILTER: ""

    steps:
      - uses: actions/checkout@v4
      
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: release

      - uses: r-lib/actions/setup-tinytex@v2

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          python-version: '3.12'
          
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          cache: always
          extra-packages: any::rcmdcheck, any::devtools
          needs: check

      - name: Pin Andromeda
        if: ${{ matrix.andromeda != 'current' }}
        shell: Rscript {0}
        run: |
          if (!requireNamespace("remotes", quietly = TRUE)) {
            install.packages("remotes")
          }
          remotes::install_version(
            "Andromeda",
            version = "${{ matrix.andromeda }}",
            repos = "https://cloud.r-project.org",
            upgrade = "never"
          )

      - name: setup r-reticulate venv
        shell: bash
        run: |
          uv pip install polars tqdm pyarrow duckdb nvidia-ml-py numpy
          uv pip install "torch==2.10.0" --index https://download.pytorch.org/whl/cpu/

      - name: Install package
        shell: bash
        run: R CMD INSTALL --no-multiarch --with-keep.source .

      - name: Run scenario
        shell: bash
        run: |
          set -euxo pipefail
          case "${{ matrix.scenario }}" in
            package-load)
              Rscript -e 'library(DeepPatientLevelPrediction); reticulate::import("torch"); cat("package-load complete\n"); invisible(gc())'
              ;;
            setup-through-dataset)
              DPLP_SETUP_TRACE=true DPLP_SETUP_STOP_AFTER=dataset Rscript tests/testthat.R
              ;;
            setup-before-fit)
              DPLP_SETUP_TRACE=true DPLP_SETUP_STOP_AFTER=before-fit Rscript tests/testthat.R
              ;;
            setup-after-fit)
              DPLP_SETUP_TRACE=true DPLP_SETUP_STOP_AFTER=after-fit Rscript tests/testthat.R
              ;;
            setup-after-save)
              DPLP_SETUP_TRACE=true DPLP_SETUP_STOP_AFTER=after-save Rscript tests/testthat.R
              ;;
            full-tests)
              Rscript tests/testthat.R
              ;;
            setup-after-save-andromeda-1.2.0)
              DPLP_SETUP_TRACE=true DPLP_SETUP_STOP_AFTER=after-save Rscript tests/testthat.R
              ;;
            full-tests-andromeda-1.2.0)
              Rscript tests/testthat.R
              ;;
          esac
