# Use polars instead of pandas for default conversion from python to R

Use polars instead of pandas for default conversion from python to R

## Usage

``` r
# S3 method for class 'polars.dataframe.frame.DataFrame'
py_to_r(x)
```

## Arguments

- x:

  A polars DataFrame object

## Value

The same data.frame in R
